<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.SalesManager.SalesChanceManage">

    <select id="selectSalesChanceByNameOrContactPerson" resultType="com.example.entity.SalChance">
        SELECT * FROM sal_chance
        WHERE chc_cust_name LIKE CONCAT('%', #{name}, '%')
           OR chc_linkman LIKE CONCAT('%', #{contactPerson}, '%')
    </select>

    <insert id="insertSalesChance" parameterType="com.example.entity.SalChance">
        INSERT INTO sal_chance (chc_cust_name,
                                chc_source,
                                chc_rate,
                                chc_linkman,
                                chc_tel,
                                chc_title,
                                chc_desc,
                                chc_create_id,
                                chc_create_by,
                                chc_create_date,
                                chc_status)
        VALUES (#{chcCustName}, #{chcSource}, #{chcRate}, #{chcLinkman}, #{chcTel}, #{chcTitle}, #{chcDesc}, #{chcCreateId}, #{chcCreateBy}, #{chcCreateDate}, 1)
    </insert>

    <update id="editSalesChance">
        UPDATE sal_chance
        SET chc_source = #{salChance.chcSource},
            chc_rate = #{salChance.chcRate},
            chc_linkman = #{salChance.chcLinkman},
            chc_tel = #{salChance.chcTel},
            chc_title = #{salChance.chcTitle},
            chc_desc = #{salChance.chcDesc},
            chc_cust_name = #{salChance.chcCustName}
        WHERE chc_id = #{id}
    </update>

    <delete id="deleteSalesChance" parameterType="int">
        DELETE FROM sal_chance WHERE chc_id = #{id}
    </delete>

    <!-- 查询销售机会状态（1未指派 2已指派 3开发成功 4终止开发） -->
    <select id="judgeSalesChanceStatus" resultType="int">
        select sal_chance.chc_status from sal_chance where chc_id = #{id}
    </select>
</mapper>
